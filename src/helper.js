import React, { Component } from 'react';

export default class Helper extends Component {
	render() {
		return (
			<div>
				this app will get the data from any api and show the previous search and when click in it it show data
				again
			</div>
		);
	}
}
